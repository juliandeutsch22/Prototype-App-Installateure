# Dependencies
node_modules
functions/node_modules

# Build output
dist
functions/lib
*.tsbuildinfo
# Von `tsc -b` aus vite.config.ts erzeugte Artefakte
vite.config.js
vite.config.d.ts

# Environment / Secrets — NIE committen
.env
.env.local
.env.*.local
functions/.env
functions/.env.local
functions/.env.*.local
!.env.example
!functions/.env.example
*.serviceaccount.json

# Firebase
.firebase
firebase-debug.log
firestore-debug.log
ui-debug.log

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
*.log

# Lokale Emulator-Secrets
functions/.secret.local

# Service-Account-Schlüssel (Bootstrap/Produktion) — NIE committen
serviceAccount*.json
*serviceaccount*.json

# Aus shared/ erzeugte Kopien — siehe functions/scripts/shared-uebernehmen.mjs
functions/src/generated

# ProjectAtlas: lokaler Index (SQLite, 32 MB) — gehoert nicht ins Repo.
# Neu erzeugt mit `projectatlas init`. Die MCP-Konfiguration bleibt dagegen
# versioniert, damit jeder Agent denselben Einstieg findet.
.projectatlas/*.db
.projectatlas/*.db-shm
.projectatlas/*.db-wal
.projectatlas/*.lock
.projectatlas/*.toon
# Die je Host erzeugten Kopien der MCP-Konfiguration tragen absolute Pfade
# DIESER Maschine. Versioniert ist nur `.mcp.json`.
.projectatlas/projectatlas.claude.mcp.json
.projectatlas/projectatlas.mcp.json
.projectatlas/projectatlas.opencode.json
supabase/functions/_shared/

# Durchklick (Playwright): Bilder, Spuren und Berichte eines Laufs. Sie sind
# Diagnose, kein Quelltext — und ein Trace-Archiv wiegt Megabyte.
test-results/
playwright-report/

# Vorschau: die Ersatzmodule werden aus src/lib/db erzeugt (siehe
# tools/vorschau/README.md) und gehoeren deshalb nicht ins Repo.
tools/vorschau/db/
